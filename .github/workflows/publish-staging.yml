name: Publish Subgraph to Registry - Staging Variant

on:
  workflow_run:
    workflows: ['Deploy Subgraph to Staging']
    types:
      - completed

jobs:
  build:
    runs-on: ubuntu-latest
    environment: apollo
    env:
      APOLLO_KEY: ${{ secrets.APOLLO_KEY }}
      APOLLO_VCS_COMMIT: ${{ github.event.pull_request.head.sha }}
      GRAPH_REF: airlock-managed-fed
      SUBGRAPH: accounts
    steps:
      - uses: actions/checkout@v2
      - name: Install Rover
        run: |
          curl -sSL https://rover.apollo.dev/nix/v0.1.0 | sh

          # Add Rover to the $GITHUB_PATH so it can be used in another step
          # https://docs.github.com/en/actions/reference/workflow-commands-for-github-actions#adding-a-system-path
          echo "$HOME/.rover/bin" >> $GITHUB_PATH
      - name: Publish subgraph to production registry
        run: |
          rover subgraph publish ${GRAPH_REF}@staging-test --schema ./${SUBGRAPH}.graphql --name $SUBGRAPH
